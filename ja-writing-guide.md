# 日本語ドキュメントの書き方（xCOMET MCP Server）

対象: `README.ja.md` および、今後生成する日本語の説明文。
作成: 2026-09-02。`README.ja.md` の改稿時に抽出した繰り返しパターンを記録する。

英語 README をそのまま日本語に置き換えると、次のずれが出やすい。

- 英語の見出し語や注記ラベルが残る（`Note`、`Tip`、`Performance Note`）
- 動詞のあとに全角コロン「：」を置き、英語の列挙の形を残す
- 英語 1 語を漢字 1 語に載せて、日本語としては指すものが定まらない
- 「提供する」「実現する」「サポートする」が主語を増やさずに繰り返される
- です・ますと、体言止めと、だ・である が同一段落で混ざる

英語版の事実（バージョン番号、パス、環境変数名、数値）は変えない。変えるのは日本語の形だけである。

---

## 書く前の 3 つの確認

1 つでも当たったら書き直す。

### 1. 逆翻訳

その語を英語に戻すと専門語 1 語になるか。なるなら英語の直訳であり、日本語としては対象を指していない。

- ×「ゲート付きモデル」← gated model
- ○「Hugging Face の gated model。リポジトリを開く前に利用承認が必要」
- ×「永続サーバーアーキテクチャ」← persistent server architecture
- ○「終了せず動き続ける Python ワーカー。モデルをメモリ上に残す」

### 2. 錨

直前の会話が無い状態で読んでも、同じ対象を指すか。フィールド名、環境変数、コマンド、エラー文があるなら、その名前で書く。

- ×「認証する。CLI でログインするか：」
- ○「認証する。方法は次の 2 つです。CLI で `hf auth login` するか、MCP ホストの `env` に `HF_TOKEN` を置く。」

### 3. 名前

指している対象に、実行すれば目に見える名前があるか。あるならその名前で書く。

- ×「フォールバック」
- ○「不正な値は `1` として扱う」（対象は `XCOMET_NUM_WORKERS`）

一語が 2 つを指していたら分けて書く。

- ×「広告と動作が食い違う」
- ○「tools/list は拒否すると書いているのに、実際は受け取って捨てる」

---

## 用語集

左は使わない形、右は使う形。英語の技術用語はカタカナまたは原語のまま残してよい。無理に漢字 1 語に載せない。

| 使わない | 使う | 理由 |
|---|---|---|
| Note | 注記 | 英語ラベルを残さない。注意（やってはいけない・漏れると動かない）と注記（補足）を分ける |
| Tip | 補足 | 「ヒント」も可。同一文書内でどちらか一方に揃える |
| Performance Note | 処理時間 | 何の注記かを名詞で書く |
| レスポンス | 応答 | HTTP 文脈以外では「応答」。例示なら「応答例」 |
| パラメータ（ツール引数の見出し） | 引数 | モデルの parameter 数は「パラメータ数」のまま |
| デフォルト | 既定 / 既定値 | 表見出しは「既定値」。文中は「既定は `false`」 |
| ゲート付きモデル | gated model（必要なら説明を添える） | 「ゲート」は門の比喩。承認手順がある事実を書く |
| 永続サーバー / 永続ワーカーアーキテクチャ | 常駐 Python ワーカー | 「永続」はディスク保存にも読める。プロセスが終了しないことを書く |
| メモリ常駐 | メモリ上に保持する / メモリ上に残る | 常駐はプロセス、保持はモデル。主語を分ける |
| プリロード / 即時ロード | 起動時の事前読み込み | 対象は `XCOMET_PRELOAD=true`。見出しと本文で語を揃える |
| フォールバック | 「A が無いときは B を使う」「不正な値は `1` として扱う」 | 何が、どの値に、どの条件で変わるかを書く |
| オプション GPU アクセラレーション | GPU を使うと推論時間を短くできる（任意） | 「アクセラレーション」は結果を書いていない |
| 品質スコアリング | 品質スコア | 「-ing」を名詞に残さない |
| 連携するように設計されています | 他の MCP サーバーと一緒に使える | 「設計されています」は主体が文書の外に逃げる |
| サポート（機能の有無） | 「できる / できない / 使う」 | 「GPU サポート」→「GPU で推論できる」 |
| サポートを受ける | 問い合わせ | 見出しが何の行為かを書く |
| 活用する | 常駐ワーカーを前提にする | 「活用」は何が起きるかを書いていない |
| 実現する | 具体的な数値や動作 | 「177 倍の高速化を実現」→「約 177 倍速くなる」 |
| こちらになります | こちらを使う | 「なります」は結論をぼかす |
| 動きます（コマンド） | 使えます | コマンドは実行できるかどうかを書く |
| 壊れにくい | Homebrew 更新後もリンク先が残りやすい | 「壊れる」はリンク切れ、実行不能、不整合のどれかを書く |
| 内容の崩壊 | 原文との対応が失われた出力 | collapse の直訳。何が失われるかを書く |
| 素通り | 未翻訳のまま残った箇所 | passthrough の直訳 |
| トリアージ | 仕分け | 医療用語を一般説明に使わない。手順を書くなら「どのセグメントから見直すか」 |
| 信号 | 判断材料 | signal の直訳 |
| 飽和して | 上限付近に寄り、差が付かなくなる | 何の値がどうなるかを書く |
| メモリスパイク | 呼び出しのたびにメモリ使用量が急増する | spike の直訳 |
| リリース判定 | リリース可否の判定 | 「判定」の対象を残す |
| 各スイートが何を守っているか | 各テストスイートの対象 | 「守る」は比喩 |
| 提供します（連続） | できること / 返すもの を列挙する | 同一段落で「提供」を繰り返さない |
| 最高のパフォーマンス | 処理時間を短くする | 比較対象が無い「最高」は使わない |

残してよい語（この文書で定義済み、または技術用語として定着）。

- MCP、JSON-RPC、venv、wheel、checkpoint / チェックポイント
- gated model（初出で「利用承認が必要」を添える）
- ポストエディット（翻訳評価の用語）
- スループット（処理時間の節で、単位付きの話と併用する）
- エラースパン、MQM、DataLoader
- Hugging Face、huggingface_hub、`hf auth login`、`HF_TOKEN`
- テストが落ちる

---

## 文の形

### 列挙の前

- ×「以下の機能を提供します：」
- ○「次のことができます。」のあと箇条書き
- ○「手順は次のとおりです。」

動詞の直後の全角コロンは使わない。箇条書きが続くときは句点で止める。

### 注記の種類

| ラベル | 使うとき |
|---|---|
| 重要 | 認証、参照訳の必須、ディスク容量など、省略すると動かないかデータを壊す |
| 注意 | 設定漏れ、既定値、環境の前提 |
| 注記 | バージョン境界、実装の変更（例: v0.5.0 で FastAPI が不要） |
| 補足 | 省略可能な設定、代替手段 |

`Note` と `注意` を混在させない。

### 主語

- サーバー、ワーカー、モデル、ホスト、利用者を取り違えない。
- 「モデルはメモリ上に残る」「ワーカーは終了しない」は別の文にする。
- 「自動的に回復します」より、「ヘルスチェックが 3 回連続で失敗すると再起動する」と手順を書く。

### 数字と範囲

- 範囲は「3.9〜3.12」「0.7〜0.9」。ハイフンと波ダッシュを混在させない。
- 「約 14GB」と「43GB」のように同一対象の概数が節ごとに違う場合は、英語版の当該箇所に合わせ、勝手に丸めない。
- 「177 倍」は英語版にある実測比較なので残す。新たに倍率を作らない。

### コードと UI の名前

- 環境変数、ツール名、エラー文、パスはバッククォートで原文のまま書く。
- エラー見出しは `"No module named 'comet'"` のように、実際の文字列に合わせる。
- 「Python パス」は `XCOMET_PYTHON_PATH` が指す実行ファイルのパスである。PATH 環境変数と混ぜない。

### です・ます

README 本文は「です・ます」に揃える。手順の箇条書きは「〜する」で止めてよい。だ・である調は、論文の引用要約を除き使わない。

---

## README.ja.md で直した箇所（2026-09-02）

事実は変えていない。形だけ変えた。

1. 冒頭「関係ありません」の主語を「本リポジトリ」にした。
2. 概要の「提供します」重複をやめ、できることを列挙した。
3. 「品質スコアリング」「GPU サポート」「オプション GPU アクセラレーション」を動作の記述に戻した。
4. 「ゲート付きモデル」を gated model と承認手順に分けた。「認証する。CLI でログインするか：」を 2 手段の列挙にした。
5. 「こちらになります」「動きます」を「こちらを使う」「使える」にした。
6. 「venv の配下には入りません」を「venv の中には入りません」にした。「配下」は組織図の比喩になる。
7. 「この分離から言えることが 3 つあります」を「別ディレクトリであることの結果は次の 3 点」にした。
8. 「モデルは再ダウンロードになりません」を「再度ダウンロードする必要はありません」にした。
9. 「永続サーバーアーキテクチャ」「即時ロード」を「常駐 Python ワーカー」「起動時の事前読み込み」にした。`XCOMET_PRELOAD` との対応を見出しに残した。
10. 「パフォーマンスノート」を「処理時間」にした。
11. ツール表の「パラメータ」「デフォルト」「レスポンス」を「引数」「既定」「応答例」にした。
12. 「連携するように設計されています」を「一緒に使える」にした。
13. 「フォールバック」を「不正な値は `1` として扱う」にした。
14. 「内容の崩壊」「素通り」「トリアージ」「信号」「飽和」を、何が起きるかに書き換えた。
15. 「各スイートが何を守っているか」を「各テストスイートの対象」にした。
16. 「サポートを受ける」を「問い合わせ」にした。
17. 「メモリスパイクを回避」を「呼び出しのたびにメモリ使用量が急増することを避ける」にした。
18. 「壊れにくい形」を「Homebrew 更新後もリンク先が残りやすい」にした。
19. 英語見出し絵文字は日本語版に入れない（英語版の装飾を機械コピーしない）。

残した箇所。

- スコア解釈の実測表と Rei et al., 2023 の引用。対象と数値が具体であるため、骨格は維持した。
- コマンド、JSON、パス、モデル ID。
- 「ポストエディット」。翻訳評価の用語として定着している。

---

## 今後の生成手順

1. 英語版の該当節を先に読む。数値、コマンド、環境変数を控える。
2. 日本語は英語の語順をコピーしない。主語（誰が）、対象（何を）、結果（何が残るか / 何が返るか）の順で書く。
3. 書き終わったら本ファイルの用語集で検索する。左列が残っていたら右列に置換する。
4. 3 つの確認を段落ごとに行う。
5. 英語版に無い倍率、評価、推奨を足さない。

英語から新規に訳すときの変換例。

```
EN: The server automatically recovers from failures.
NG: サーバーは障害から自動的に回復します。
OK: ワーカーが応答しなくなったときの動作です。ヘルスチェックが 3 回連続で失敗すると再起動します。再起動を 3 回試したあとは停止します。
```

```
EN: gated models on HuggingFace
NG: HuggingFace のゲート付きモデル
OK: Hugging Face の gated model。リポジトリを開く前に、Hugging Face 側で利用承認が必要です。
```

```
EN: persistent Python worker that keeps the model loaded in memory
NG: モデルをメモリに常駐させる永続 Python ワーカー
OK: 終了せず動き続ける Python ワーカー。xCOMET モデルをメモリ上に載せたままにする。
```

---

## ファイル

| パス | 役割 |
|---|---|
| `README.ja.md` | 公開する日本語 README（改稿済み） |
| `ja-writing-guide.md` | 本ファイル。用語と点検 |
| 上流 | https://github.com/shuji-bonji/xcomet-mcp-server/blob/main/README.ja.md |
